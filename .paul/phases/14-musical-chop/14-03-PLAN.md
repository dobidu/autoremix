---
phase: 14-musical-chop
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - python/server/presets/trap_stutter.json
  - python/server/presets/onset_drill.json
  - python/server/presets/structural_loop.json
autonomous: true
---

<objective>
## Goal
Create 3 built-in preset JSON files that exercise the 5 new ops from Plan 14-02, demonstrating musically-intelligent chopping in real remix configurations.

## Purpose
Without concrete presets, the new ops are unreachable from the plugin UI (style_combo_ only shows loaded presets). These 3 presets make the Phase 14 ops immediately usable and serve as reference examples for custom preset authors.

## Output
- `python/server/presets/trap_stutter.json`
- `python/server/presets/onset_drill.json`
- `python/server/presets/structural_loop.json`

All 3 auto-loaded by `PresetLoader` — no Python changes needed.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/14-musical-chop/14-02-SUMMARY.md

## Source Files
@python/server/presets/chopped_screwed.json
@python/server/presets/loader.py
@python/server/remix/chain_interpreter.py
</context>

<acceptance_criteria>

## AC-1: trap_stutter loads and validates
```gherkin
Given python/server/presets/trap_stutter.json exists
When PresetLoader().load_all() is called
Then "trap_stutter" key present in result dict
  And preset.effects contains at least 3 entries using chop_beats, chop_onsets, bass_boost
```

## AC-2: onset_drill loads and validates
```gherkin
Given python/server/presets/onset_drill.json exists
When PresetLoader().load_all() is called
Then "onset_drill" key present in result dict
  And preset.effects contains at least 2 entries using chop_onsets, gate_energy
```

## AC-3: structural_loop loads and validates
```gherkin
Given python/server/presets/structural_loop.json exists
When PresetLoader().load_all() is called
Then "structural_loop" key present in result dict
  And preset.effects contains at least 2 entries using structural_cut
```

## AC-4: All 6 presets appear in GET /api/v1/presets
```gherkin
Given the sidecar is NOT running (offline test)
When PresetLoader().load_all() is called directly
Then result has exactly 6 keys: chopped_screwed, slowed_reverb, drum_and_bass,
     trap_stutter, onset_drill, structural_loop
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create trap_stutter.json</name>
  <files>python/server/presets/trap_stutter.json</files>
  <action>
    Trap/hip-hop beat stutter: slow tempo, half-beat vocal stutter, onset-locked drum repeat, bass boost.

    ```json
    {
      "id": "trap_stutter",
      "version": "1.0",
      "name": "Trap Stutter",
      "description": "Trap-style half-beat vocal stutter with onset-locked drum hits and bass boost.",
      "author": "AutoRemix",
      "tags": ["trap", "hip-hop", "stutter", "chop"],
      "engine": null,
      "params": {
        "tempo_factor": 0.85,
        "pitch_shift_semi": -2.0,
        "reverb_mix": 0.0,
        "chop_interval_ms": 0.0,
        "bass_boost_db": 0.0,
        "drums_tempo_factor": 1.0
      },
      "stem_mix": { "vocals": 1.2, "drums": 1.0, "bass": 1.1, "other": 0.8 },
      "effects": [
        { "op": "time_stretch", "stems": ["drums", "bass", "other"], "params": { "factor": 0.85 } },
        { "op": "pitch_shift",  "stems": ["vocals", "bass"],         "params": { "semitones": -2.0 } },
        { "op": "chop_beats",   "stems": "vocals",                   "params": { "division": 0.5, "repeat": 3, "offset_beats": 0 } },
        { "op": "chop_onsets",  "stems": "drums",                    "params": { "min_gap_ms": 80, "threshold": 0.35, "repeat": 2 } },
        { "op": "bass_boost",   "stems": "bass",                     "params": { "db": 4.0 } }
      ]
    }
    ```

    Note: `"engine": null` — effects array drives processing via EffectChainEngine.
  </action>
  <verify>cd /home/bidu/autoremix/autoremix/python && source .venv/bin/activate && python -c "
from server.presets.loader import PresetLoader
p = PresetLoader().load_all()
assert 'trap_stutter' in p, 'trap_stutter not loaded'
ops = [e['op'] for e in p['trap_stutter'].effects]
assert 'chop_beats' in ops and 'chop_onsets' in ops, f'missing ops: {ops}'
print('trap_stutter OK, ops:', ops)
"</verify>
  <done>AC-1 satisfied: trap_stutter loads with chop_beats and chop_onsets in effects</done>
</task>

<task type="auto">
  <name>Task 2: Create onset_drill.json</name>
  <files>python/server/presets/onset_drill.json</files>
  <action>
    UK drill / grime style: tight onset chopping on vocals, energy gating on atmosphere, offset bass hits.

    ```json
    {
      "id": "onset_drill",
      "version": "1.0",
      "name": "Onset Drill",
      "description": "UK drill-inspired tight onset chopping on vocals with energy-gated atmosphere.",
      "author": "AutoRemix",
      "tags": ["drill", "grime", "uk", "chop", "stutter"],
      "engine": null,
      "params": {
        "tempo_factor": 1.0,
        "pitch_shift_semi": 0.0,
        "reverb_mix": 0.0,
        "chop_interval_ms": 0.0,
        "bass_boost_db": 0.0,
        "drums_tempo_factor": 1.0
      },
      "stem_mix": { "vocals": 1.0, "drums": 1.2, "bass": 1.1, "other": 0.7 },
      "effects": [
        { "op": "chop_onsets",  "stems": "vocals",  "params": { "min_gap_ms": 60, "threshold": 0.25, "repeat": 2 } },
        { "op": "gate_energy",  "stems": "other",   "params": { "threshold_db": -28, "hold_ms": 30 } },
        { "op": "chop_beats",   "stems": "bass",    "params": { "division": 1.0, "repeat": 2, "offset_beats": 1 } },
        { "op": "eq_highpass",  "stems": "other",   "params": { "cutoff_hz": 300 } }
      ]
    }
    ```
  </action>
  <verify>cd /home/bidu/autoremix/autoremix/python && source .venv/bin/activate && python -c "
from server.presets.loader import PresetLoader
p = PresetLoader().load_all()
assert 'onset_drill' in p, 'onset_drill not loaded'
ops = [e['op'] for e in p['onset_drill'].effects]
assert 'chop_onsets' in ops and 'gate_energy' in ops, f'missing ops: {ops}'
print('onset_drill OK, ops:', ops)
"</verify>
  <done>AC-2 satisfied: onset_drill loads with chop_onsets and gate_energy in effects</done>
</task>

<task type="auto">
  <name>Task 3: Create structural_loop.json</name>
  <files>python/server/presets/structural_loop.json</files>
  <action>
    Experimental structural rearrangement: segments of vocals and atmosphere play in reverse order, drums/bass slow slightly, reverb wash on vocals.

    ```json
    {
      "id": "structural_loop",
      "version": "1.0",
      "name": "Structural Loop",
      "description": "Reverses song structure section-by-section. Vocals and atmosphere play in reverse segment order with reverb wash.",
      "author": "AutoRemix",
      "tags": ["experimental", "structural", "reverse", "ambient"],
      "engine": null,
      "params": {
        "tempo_factor": 0.90,
        "pitch_shift_semi": -1.0,
        "reverb_mix": 0.0,
        "chop_interval_ms": 0.0,
        "bass_boost_db": 0.0,
        "drums_tempo_factor": 1.0
      },
      "stem_mix": { "vocals": 1.0, "drums": 0.9, "bass": 1.0, "other": 1.1 },
      "effects": [
        { "op": "time_stretch",    "stems": ["drums", "bass"], "params": { "factor": 0.90 } },
        { "op": "structural_cut",  "stems": "vocals",          "params": { "n_segments": 4, "mode": "reverse" } },
        { "op": "structural_cut",  "stems": "other",           "params": { "n_segments": 4, "mode": "reverse" } },
        { "op": "reverb",          "stems": "vocals",          "params": { "mix": 0.25, "room_size": 0.7 } },
        { "op": "pitch_shift",     "stems": ["vocals", "other"], "params": { "semitones": -1.0 } }
      ]
    }
    ```

    Note: structural_cut applied individually per stem — slight length variance between stems is acceptable; chain_interpreter trims to min_len when mixing.
  </action>
  <verify>cd /home/bidu/autoremix/autoremix/python && source .venv/bin/activate && python -c "
from server.presets.loader import PresetLoader
p = PresetLoader().load_all()
assert 'structural_loop' in p, 'structural_loop not loaded'
ops = [e['op'] for e in p['structural_loop'].effects]
assert ops.count('structural_cut') >= 2, f'expected 2x structural_cut, got: {ops}'
print('structural_loop OK, ops:', ops)
# verify all 6 presets present
expected = {'chopped_screwed','slowed_reverb','drum_and_bass','trap_stutter','onset_drill','structural_loop'}
assert expected == set(p.keys()), f'preset mismatch: {set(p.keys())}'
print('All 6 presets loaded:', sorted(p.keys()))
"</verify>
  <done>AC-3 and AC-4 satisfied: structural_loop loads; all 6 presets present</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- python/server/remix/ops.py (complete from 14-02)
- python/server/remix/analysis.py (complete from 14-01)
- python/server/presets/loader.py
- python/server/presets/chopped_screwed.json
- python/server/presets/slowed_reverb.json
- python/server/presets/drum_and_bass.json
- python/server/presets/schema.json
- Any test files from prior plans

## SCOPE LIMITS
- JSON files only — no Python code changes
- No new HTTP endpoints
- No C++ changes (that's Plan 14-04)
- Do not modify existing 3 preset files
</boundaries>

<verification>
Before declaring plan complete:
- [ ] python -c "from server.presets.loader import PresetLoader; p=PresetLoader().load_all(); assert len(p)==6; print(sorted(p.keys()))"
- [ ] All 3 new JSON files parse without error via model_validate_json
- [ ] Each new preset has non-empty effects array with only valid op names from OP_REGISTRY
</verification>

<success_criteria>
- All 3 tasks completed
- 6 total presets load cleanly (3 existing + 3 new)
- Each new preset exercises at least one op from Plan 14-02
- No modifications to existing presets or Python code
</success_criteria>

<output>
After completion, create `.paul/phases/14-musical-chop/14-03-SUMMARY.md`
</output>
